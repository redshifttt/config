#!/bin/sh

choice=$(printf "suspend\nshutdown\nreboot\n" | bemenu -s -W 0.2 -c -l 3 -p "power actions ")

case "${choice}" in
    suspend) sudo systemctl suspend ;;
    shutdown) sudo shutdown now ;;
    reboot) sudo reboot ;;
esac
